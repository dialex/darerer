<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="colorPrimary">@color/colorBackgroundPrimary</item>
        <item name="colorPrimaryDark">@color/colorBackgroundPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:textColorPrimary">@color/colorPrimaryText</item>
        <item name="android:windowBackground">@color/colorBackground</item>
        <item name="android:navigationBarColor">@color/colorBackgroundPrimaryDark</item>
    </style>

    <style name="AppKindTheme" parent="AppTheme">
        <item name="colorAccent">@color/colorKindPrimary</item>
    </style>

    <style name="AppNiceTheme" parent="AppTheme">
        <item name="colorAccent">@color/colorNicePrimary</item>
    </style>

    <style name="AppNaughtyTheme" parent="AppTheme">
        <item name="colorAccent">@color/colorNaughtyPrimary</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

</resources>
